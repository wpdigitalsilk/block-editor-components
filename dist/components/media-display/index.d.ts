export function MediaDisplay({ media, isBackground, ...rest }: {
    media: object;
    isBackground: boolean;
    rest: object;
}): JSX.Element;
export namespace MediaDisplay {
    namespace propTypes {
        let media: PropTypes.Validator<object>;
        let isBackground: PropTypes.Requireable<boolean>;
    }
}
import PropTypes from 'prop-types';
//# sourceMappingURL=index.d.ts.map